# explorer/ — NUTRIDB Explorer (SPEC §12)

Aplicação web **estática** (React + TypeScript + Vite, TanStack Router/Table, Tailwind).

- F1.8b: página mínima de pesquisa (sql.js/WASM, sem HTTP-range)
- F8: as 13 vistas completas (SQLite WASM + pedidos HTTP por intervalo de bytes)

Ver skill `frontend-design` antes de escrever a UI.