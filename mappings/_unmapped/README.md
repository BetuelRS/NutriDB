# mappings/_unmapped/ — gerado pelo build

O build escreve aqui os códigos de fonte que ainda não têm mapeamento (um CSV por fonte).
**Gate de qualidade**: este diretório tem de estar vazio nos critérios de aceitação (SPEC §11).

O conteúdo é gitignored — apenas este README é versionado.