# reference/ — valores de referência dietéticos

Esperado (F2+): `efsa_drv.csv`, `iom_dri.csv`, `nnr.csv`, `who_fao.csv`.

Guardados por (nutriente, sexo, faixa etária, estado fisiológico, autoridade) — nunca um único número global.